import type { PlantProfile } from "./plantTypes";

/**
 * Extra curated profiles (merged after `corePlantProfiles` in `plantProfiles.ts`).
 * Add entries here anytime—unknown names still fall back to Wikipedia when wired in App.
 */
export const extraPlantProfiles: PlantProfile[] = [];
